<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.web>

    <!-- Regra de autorização de acesso -->
    <!-- ? Usuário anônimo -->
    <authorization>
      <deny users="?"/>
    </authorization>
    
  </system.web>
</configuration>
